# Υποβολή Challenge για τη θέση Web Developer - XE.gr

Αυτό το repository περιέχει την υποβολή μου για το coding challenge της XE.gr. [cite_start]Ο στόχος ήταν η κατασκευή μιας full-stack εφαρμογής που επιτρέπει σε έναν χρήστη να δημιουργήσει μια νέα αγγελία ακινήτου. [cite: 15]

[cite_start]Η εφαρμογή αποτελείται από ένα backend API σε Node.js/Express και ένα frontend σε React [cite: 17][cite_start], με ιδιαίτερη έμφαση στη λειτουργικότητα της επιλογής περιοχής μέσω ενός autocomplete API. [cite: 19]

## Τεχνολογίες που χρησιμοποιήθηκαν

- **Frontend:** React (Vite), Axios
- **Backend:** Node.js, Express.js
- **Database:** MongoDB (μέσω του Atlas cloud service)
- **Library για επικοινωνία με DB:** Mongoose
- **Development Tools:** Nodemon, Git & GitHub

## Δομή του Project

Το project ακολουθεί μια **monorepo** αρχιτεκτονική, με τον κώδικα να είναι οργανωμένος σε δύο βασικούς φακέλους:
- `/backend`: Περιέχει τον Node.js/Express server.
- `/frontend`: Περιέχει την React εφαρμογή.

## Οδηγίες Εγκατάστασης & Εκτέλεσης

Για να τρέξετε το project τοπικά, θα χρειαστείτε:
- Node.js (v18 ή νεότερη)
- Git
- Έναν δωρεάν λογαριασμό στο MongoDB Atlas

### Βήματα

1.  **Κάντε clone το repository:**
    ```bash
    git clone [https://github.com/YourUsername/YourRepoName.git](https://github.com/YourUsername/YourRepoName.git)
    cd YourRepoName
    ```

2.  **Ρυθμίστε το Backend:**
    ```bash
    # Πηγαίνετε στον φάκελο του backend
    cd backend

    # Δημιουργήστε ένα αρχείο .env και προσθέστε το connection string σας
    # (αντιγράψτε το .env.example αν υπάρχει)
    echo "MONGO_URI=YOUR_MONGODB_ATLAS_CONNECTION_STRING" > .env

    # Εγκαταστήστε τις εξαρτήσεις
    npm install

    # Ξεκινήστε τον backend server
    npm run dev
    ```
    *Ο server θα τρέχει στο `http://localhost:5000`.*

3.  **Ρυθμίστε το Frontend (σε νέο terminal):**
    ```bash
    # Από τον κεντρικό φάκελο, πηγαίνετε στο frontend
    cd frontend

    # Εγκαταστήστε τις εξαρτήσεις
    npm install

    # Ξεκινήστε τον frontend development server
    npm run dev
    ```
    *Η εφαρμογή θα είναι ορατή στο `http://localhost:5173` (ή σε όποια πόρτα αναφέρει το Vite).*

## Υλοποιημένα Features

- Full-stack εφαρμογή frontend και backend.
- [cite_start]Δημιουργία φόρμας με "controlled components" σε React. [cite: 18]
- [cite_start]Δυναμικό autocomplete πεδίο που επικοινωνεί με το API της XE. [cite: 19]
- Βελτιστοποίηση των API calls με τεχνική **debouncing** στο frontend.
- [cite_start]**[Bonus]** Αποθήκευση των δεδομένων της αγγελίας σε cloud βάση δεδομένων MongoDB. [cite: 29]
- [cite_start]**[Bonus]** Υλοποίηση **in-memory caching** στο backend για το autocomplete endpoint για μείωση των κλήσεων. [cite: 30]
- [cite_start]**[Bonus]** Το styling της φόρμας είναι **mobile-first** και πλήρως responsive. [cite: 28]